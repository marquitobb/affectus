<h2 class="mt-5"><%=nombrePagina %></h2>
<div class="container mt-5">
    <form method="POST" enctype="multipart/form-data">
        <div class="form-row">
            <div class="form-group col-md-12">
                <label for="file">Subir Archivos de Estrategia</label>
                <input type="file" class="form-control" id="archivos" name="cv">
            </div>
            <div class="form-group">
                <% if(usuario.cv) {%>
                    <a class="nada" href="" data-toggle="modal" data-target=".bd-example-modal-lg">
                        <i class="tamano far fa-file-pdf">
                        </i> 
                    </a>
                    <p>Ver CV</p>                    
                <% } else { %>
                    <p>No tienes cargado ningun archivo aún</p>
                <% } %>
            </div>
        </div>
        <div class="form-group">
            <input type="submit" class="btn btn-primary float-right" value="Subir Imagen"></input>
        </div>
  </form>
</div>

<%- include('partials/modal-cv.ejs') %>;